export class WeatherTranslated {
    public constructor(title, description, icon) {
        this.title = title;
        this.description = description;
        this.icon = icon;
    }

    public title: string;
    public description: string;
    public icon: string;
}